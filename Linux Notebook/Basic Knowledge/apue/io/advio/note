
高级IO

一、非阻塞IO
	
假错：
	阻塞：EINTR
	非阻塞：EAGAIN

	有限状态机(复杂流程)
	要素：
		现态：正在处于状态	
		条件：状态更改的条件
		次态：条件变更要推动到的状态
		动作：从现态切换到次态需要执行的动作(非必要)

二、i/o多路转接
	select(2);

	poll(2);
	epoll();

三、readv() 
	writev();

四、存储映射io




